<%@ page language="java" contentType="text/html; charset=ISO-8859-1"
    pageEncoding="ISO-8859-1"%>
<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=ISO-8859-1">
<title>Insert title here</title>
</head>
<body>
<h1 class="h1">Java String</h1>
<p>In java, string is basically an object that represents sequence of char values. An array of characters works same as java string. For example:</p>

<p><strong>Java String</strong> class provides a lot of methods to perform operations on string such as compare(), concat(), equals(), split(), length(), replace(), compareTo(), intern(), substring() etc.</p>

<p>The java.lang.String class implements <i>Serializable</i>, <i>Comparable</i> and <i>CharSequence</i> interfaces.</p>

<hr/>
<h2 class="h2">CharSequence Interface</h2>
<p>The CharSequence interface is used to represent sequence of characters. It is implemented by String, StringBuffer and StringBuilder classes. It means, we can create string in java by using these 3 classes.</p>

<p>The java String is immutable i.e. it cannot be changed. Whenever we change any string, a new instance is created. For mutable string, you can use StringBuffer and StringBuilder classes.</p>

<p>We will discuss about immutable string later. Let's first understand what is string in java and how to create the string object.</p>
<hr/>
<h3 class="h2">What is String in java</h3>
<p>Generally, string is a sequence of characters. But in java, string is an object that represents a sequence of characters. The java.lang.String class is used to create string object.</p>

<a id="stringobjects"></a>
<h3 class="h3">How to create String object?</h3>
<table  >
<tr><td>There are two ways to create String object:
<ol>
<li>By string literal</li>
<li>By new keyword</li>
</ol>
</td></tr>
</table>
<hr/>
<br><center>
<form action="extratopic"   method="post"  >
<input type="submit" name="back" value="BACK TO MAIN MENU"  /></form></center>
</body>
</html>